#include <stdio.h>

int main() {
    printf("Hello world");

    int sample[] = {4, 1, 2, -3, 4};

    int combo;

    for (int outer = 0; outer < sample[0]; outer++){
        if (outer == sample[0]) {
            continue;
        }
        for (int inner = 0; inner < sample[0]; inner++) {
            
        }
    }

    return 0;
}
